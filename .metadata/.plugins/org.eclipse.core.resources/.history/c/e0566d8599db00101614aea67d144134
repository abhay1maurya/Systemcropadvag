/* =========================================================
   KRISHI SAHAYAK – BLOG PAGE CSS (FINAL & STABLE)
   ========================================================= */

/* ---------------- VARIABLES ---------------- */
:root {
    --green: #7cb62f;
    --green-dark: #5a9c1f;
    --dark: #333;
    --gray: #666;
    --light-gray: #f5f7fa;
    --border: #e5e7eb;
    --white: #ffffff;

    --shadow: 0 4px 20px rgba(0,0,0,0.08);
    --shadow-hover: 0 10px 30px rgba(124,182,47,0.2);
    --transition: all 0.3s ease;
}

/* ---------------- RESET ---------------- */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Poppins', sans-serif;
    background: #fff;
    color: var(--gray);
    line-height: 1.6;
}

/* ---------------- LAYOUT ---------------- */
.container {
    max-width: 1200px;
    margin: auto;
    padding: 0 20px;
}

/* =========================================================
   HEADER
   ========================================================= */
.blog-header-section {
    position: fixed;
    top: 0;
    width: 100%;
    background: #fff;
    z-index: 1000;
    box-shadow: 0 2px 15px rgba(0,0,0,.05);
}

.blog-header-nav {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 15px 0;
}

.blog-brand-text {
    font-size: 28px;
    font-family: 'Great Vibes', cursive;
    color: var(--green);
}

.blog-nav-links {
    display: flex;
    gap: 30px;
}

.blog-nav-link {
    text-decoration: none;
    font-weight: 600;
    color: var(--dark);
}

.blog-nav-link.active,
.blog-nav-link:hover {
    color: var(--green);
}

/* =========================================================
   HERO SECTION
   ========================================================= */
.blog-hero-section {
    margin-top: 80px;
    min-height: 60vh;
    background: linear-gradient(
        rgba(0,0,0,.6),
        rgba(0,0,0,.6)
    ), url('../img/br_blog.jpeg') center/cover no-repeat;
    display: flex;
    align-items: center;
    text-align: center;
}

.blog-hero-title {
    font-size: 3rem;
    color: #fff;
    font-weight: 800;
}

.blog-text-gradient {
    background: linear-gradient(90deg, #7cb62f, #aeea00);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
}

.blog-hero-desc {
    color: #eee;
    margin: 20px 0 40px;
}

/* =========================================================
   CATEGORIES
   ========================================================= */
.blog-categories-section {
    padding: 80px 0;
}

.blog-categories-container {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 20px;
}

.blog-category-card {
    width: 150px;
    padding: 25px;
    text-align: center;
    border-radius: 20px;
    border: 2px solid var(--border);
    cursor: pointer;
    transition: var(--transition);
    background: #fff;
}

.blog-category-card.active,
.blog-category-card:hover {
    border-color: var(--green);
    transform: translateY(-8px);
    box-shadow: var(--shadow-hover);
}

.blog-category-icon {
    font-size: 24px;
    color: var(--green);
    margin-bottom: 10px;
}

/* =========================================================
   POSTS GRID
   ========================================================= */
.blog-posts-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(330px, 1fr));
    gap: 30px;
}

/* =========================================================
   POST CARD (COMMON)
   ========================================================= */
.blog-post-card {
    background: #fff;
    border-radius: 20px;
    box-shadow: var(--shadow);
    overflow: hidden;
    transition: var(--transition);
    border: 1px solid var(--border);
}

.blog-post-card:hover {
    transform: translateY(-10px);
    box-shadow: var(--shadow-hover);
}

/* IMPORTANT:
   ❌ NO display:none here
   JS controls visibility
*/

.blog-post-image {
    height: 220px;
    overflow: hidden;
    position: relative;
}

.blog-post-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: 0.5s;
}

.blog-post-card:hover img {
    transform: scale(1.1);
}

.blog-post-tag {
    position: absolute;
    top: 15px;
    right: 15px;
    background: var(--green);
    color: #fff;
    padding: 6px 14px;
    border-radius: 20px;
    font-size: 12px;
}

/* ---------------- POST CONTENT ---------------- */
.blog-post-content {
    padding: 25px;
}

.blog-post-meta {
    display: flex;
    justify-content: space-between;
    align-items: center;
    border-bottom: 1px solid var(--border);
    padding-bottom: 15px;
    margin-bottom: 15px;
}

.blog-post-author {
    display: flex;
    gap: 10px;
    align-items: center;
}

.blog-author-img {
    width: 40px;
    height: 40px;
    border-radius: 50%;
}

.blog-post-title {
    font-size: 18px;
    font-weight: 700;
    color: var(--dark);
    margin-bottom: 10px;
}

.blog-post-excerpt {
    font-size: 14px;
    margin-bottom: 20px;
}

/* ---------------- FOOTER ---------------- */
.blog-post-footer {
    display: flex;
    justify-content: space-between;
    align-items: center;
    border-top: 1px solid var(--border);
    padding-top: 15px;
}

.blog-post-stats span {
    font-size: 13px;
    cursor: pointer;
}

.blog-read-btn {
    border: 1px solid var(--green);
    background: transparent;
    color: var(--green);
    padding: 8px 18px;
    border-radius: 50px;
    cursor: pointer;
    transition: var(--transition);
}

.blog-read-btn:hover {
    background: var(--green);
    color: #fff;
}

/* =========================================================
   FARMER’S CORNER (Load More applies)
   ========================================================= */
.farmer-post {
    display: none; /* JS will show progressively */
}

/* =========================================================
   EXPERT INSIGHTS (ALWAYS visible)
   ========================================================= */
.expert-post {
    display: block !important;
}

.expert-card-style {
    border: 1px solid #eee;
    box-shadow: none;
}

.expert-card-style:hover {
    box-shadow: 0 10px 30px rgba(0,0,0,.08);
    border-color: var(--green);
}

/* =========================================================
   LOAD MORE BUTTON
   ========================================================= */
.blog-load-more-container {
    text-align: center;
    margin-top: 40px;
}

.blog-load-btn {
    padding: 12px 35px;
    border-radius: 50px;
    border: 2px solid var(--green);
    background: #fff;
    color: var(--green);
    font-weight: 600;
    cursor: pointer;
    transition: var(--transition);
}

.blog-load-btn:hover {
    background: var(--green);
    color: #fff;
}

/* =========================================================
   MODAL
   ========================================================= */
.blog-modal {
    position: fixed;
    inset: 0;
    background: rgba(0,0,0,.6);
    display: none;
    justify-content: center;
    align-items: center;
    z-index: 2000;
}

.blog-modal.active {
    display: flex;
}

.blog-article-modal-content {
    background: #fff;
    border-radius: 20px;
    max-width: 800px;
    width: 100%;
    max-height: 90vh;
    overflow-y: auto;
    padding: 30px;
}

/* =========================================================
   RESPONSIVE
   ========================================================= */
@media (max-width: 768px) {
    .blog-hero-title {
        font-size: 2.2rem;
    }

    .blog-posts-grid {
        grid-template-columns: 1fr;
    }

    .blog-post-footer {
        flex-direction: column;
        gap: 10px;
    }

    .blog-read-btn {
        width: 100%;
    }
}
